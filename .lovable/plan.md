Сверил спеку — карточки и обёртка по размерам поплыли. Также добавляю строку поиска. Контент турниров не меняю (по прошлому уточнению — завершённые не добавляем).

## Что добавляю

**`src/components/court-count/SearchBar.tsx`** — новая строка поиска (заглушка, без обработчика):
- 336×31, padding `0 12px`, background `--court-surface-muted` (`#E1E8ED` в спеке — добавлю новый токен `--court-search-bg`, чтобы тёмная тема не отвалилась), `border-radius: 8`.
- Внутри: иконка `Search` 14×14 цвета `--court-text-soft`, плейсхолдер «Поиск по названию» (Lato 400 14/18, цвет `#929C9F` — это `--court-border`/«on-surface 400»; уже есть).
- `<input type="text" readonly>` без рамки, прозрачный фон, чтобы карет/тап работал визуально, но запросов поиска не делаем.

## Что правлю в карточке турнира

`TournamentCard.tsx` — приведу к спеке:
- ширина **336** (было 344), `border-radius: 4` (было 6), `padding: 12`, gap между заголовком и телом — **8** (было 12);
- рамка `0.5px solid var(--court-border)` — ок;
- заголовок: Lato **700, 14/17** (было 16/22);
- плашка кубка **54×51** (было 64×64), padding 8, `border-radius: 8`, background `--court-surface-muted` (`#EDF1F3`);
- иконка `Trophy` **26×26** цвета `--court-text-soft`/серый (`#D1D9DF`), **не жёлтая** — в макете кубок серый. Сделаю через `color: var(--court-trophy)` с новым токеном (`#D1D9DF` light / тёмный — серее на тёмном фоне);
- ряд «плашка ↔ тексты» gap **12**, тексты — колонка с gap **8**;
- флаг 18×14 (было 20×14);
- иконка `Calendar` 16×16 `--court-text-soft`;
- город и дата — Lato 400 14/18, цвет `--court-text-soft` (`#525556`) — было `--court-text-strong`.

## Список карточек

В `src/routes/index.tsx`:
- контейнер списка: ширина **336** (было 344), gap **8** (было 12);
- между табами и списком вставляю `SearchBar` внутри блока `{width 360, gap 16}` (как третий элемент, согласно спеке Frame 1321318242).
- `PageTitle` подгоняю под Roboto 500 **20/26**, `letter-spacing: 0.02em` (сейчас 22/26).

## Новые токены в `src/styles.css`

- `--court-search-bg`: light `#E1E8ED`, dark — тёмный аналог (`#1E2A30`).
- `--court-trophy`: light `#D1D9DF`, dark — `#3A484F` (или тот же `--court-text-soft`, посмотрю по фону).

Регистрирую их в `@theme inline` (`--color-court-search-bg`, `--color-court-trophy`).

## Что НЕ трогаю

- `MatchCard`, экран матчей, шапку, `SectionTabs` (его правки задели бы экран матчей), `CreateMatchButton`.
- Логику поиска и любой onChange.
- Список турниров и их данные.
