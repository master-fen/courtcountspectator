## Что меняем

### 1. `src/lib/mock-matches.ts`
- Расширить тип `Match` полем `status: "active" | "completed"`.
- Изменить `Player.sets` с `number[]` на `Array<{ score: number; tb?: number }>` для поддержки индекса тай-брейка (например `{ score: 6, tb: 5 }` → `6⁵`). Существующие записи перевести в новый формат.
- Для всех сетов со счётом `7:6` (как в существующих, так и в новых матчах) проставить `tb` у проигравшего тай-брейк (т.е. у того, у кого `6`). Конкретно:
  - Активный матч Бэкхендов Пётр — Эйсов Максим (сейчас 7:6 и 5:7): сет 1 → у Эйсова `{score:7}`, у Бэкхендова `{score:6, tb:?}`; сет 2 → у Бэкхендова `{score:7}`, у Эйсова `{score:5}`. Поскольку у текущих 7:6-сетов индекс тай-брейка не задан явно — проставлю правдоподобные значения (например `tb: 5`) и в комментарии помечу, что это мок-данные. Если у пользователя есть точные числа — поправим.
  - Активный Уимблдонов Игорь — Ракеткин Денис (сейчас 6:6 — это незаконченный сет, не 7:6, не трогаем).
- Добавить 5 новых матчей со `status: "completed"`:
  1. Слипкнотов Игорь — Металликов Артём, 6:4 6:3
  2. Джоковичев Николай — Надалян Армен, 6:7(?) 7:6(?) 10:8 — проставлю `tb` у проигравших каждый тай-брейк (значения мок-данные)
  3. Тайбрекова Варвара — Аутова Полина, 6:1 6:0
  4. Бэкхендов Пётр — Эйсов Максим, 4:6 6:4 7:6(5) — у Эйсова в 3-м сете `{score:6, tb:5}`
  5. Уимблдонов Игорь — Ракеткин Денис, 6:2 6:2
- Корты/стадии/таймер — по аналогии с активными.

### 2. `src/components/court-count/MatchCard.tsx`
- В рендере счёта сета поддержать новый формат `{score, tb?}`: если есть `tb` — рядом со счётом маленький `sup` (≈9–10px, тот же цвет, `position:relative`, без `vertical-align`, ломающего высоту строки и колонки сетов).
- Цвет точки в шапке: при `status === "completed"` → нейтрально-серый (`var(--court-text-soft)` или ближайший серый токен, соответствующий скриншоту). Остальные элементы шапки не трогаем.

### 3. `src/components/court-count/StatusPills.tsx`
- Сделать управляемым: пропсы `active: string`, `onChange: (v: string) => void`.
- Список: `["Все", "Активные", "Завершённые"]` (порядок как на скрине). Стили не меняем.

### 4. `src/routes/index.tsx`
- `useState<"Все" | "Активные" | "Завершённые">("Все")`, передать в `StatusPills`.
- Фильтрация `mockMatches`:
  - `Все` → активные, затем завершённые (стабильный порядок внутри групп);
  - `Активные` → только `active`;
  - `Завершённые` → только `completed`.

## Что НЕ трогаем
Header, Logo, TournamentTitle, SectionTabs, отступы/гэпы, размеры карточек, шрифты, фоны, сообщения в карточках.

## Открытый вопрос
Для существующих 7:6-сетов не известны точные индексы тай-брейка — проставлю правдоподобные мок-значения. Если нужны конкретные — пришли.
